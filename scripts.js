// JavaScript can be used for interactive features or dynamic content updates.
// For this example, the script is minimal and may be expanded as needed.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Vehicle as Code (VaC) Overview page loaded.');
});
